**
  Programming language created by Aiden Blishen Cuneo.
  First started on: 22/11/18.
**

import random, sys;

class Main {
    fun __init__ self {
        $self.get_input a = 'Enter file name: ';
        **$var a;**
        !self.importFile a;
    }

    fun get_input self prompt {
        if sys.version_info[0] < 3 {
            return `!raw_input prompt`;
        } else {
            return `!input prompt`;
        }
    }

    fun findFile self fileName {
        $var FileNotFound = `? sys.version_info[0] < 3 :: IOError | FileNotFoundError`;
        try {
            $open f = fileName;
            !f.close;
            return True;
        } catch FileNotFound {
            return False;
        }
    }

    fun importFile self fileName {
        if `!self.findFile fileName` {
            !print 'File exists.';
        } else {
            !print "File doesn't exist.";
        }
    }

    fun roundNumber self num places=0 {
        return `!round num, places`;
    }
}
